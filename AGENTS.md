# pm-tools-uniapp 开发指令

## 项目概述
本项目是一个小程序框架，内嵌H5应用。

基于 `uni-app` 开发，数据库使用 `uniCloud-alipay`。小程序通过跳转链接参数 `token` 和 `uid` 将用户信息带到H5应用内部

## 代码规范
- 保持代码简洁，避免过度设计
- 优先实现核心功能
- 修改云数据库 schema 时，字段命名需清晰、语义明确
- 新增字段时尽量兼容旧数据，避免破坏现有逻辑
- 云函数命名要清楚表达职责
- 若涉及权限、登录态、用户信息，需优先考虑安全性

## 代码风格
- 遵循项目现有代码风格
- 组件名使用 PascalCase
- 函数名使用 camelCase
- 常量使用 UPPER_SNAKE_CASE
- 保持代码简洁，避免过度设计
- 如果项目中已使用 ESLint / Prettier，则保持一致

## uniCloud 相关约定
- 修改云数据库 schema 时，应同步考虑：
  - 默认值
  - 兼容性
  - 权限配置
  - 前端读写逻辑
- 新增字段时，优先补充清晰的 `description` / `label`
- 如果字段影响登录、用户状态、会员状态、权限判断等逻辑，要同时检查相关云函数与前端页面
- 数据库字段命名应清晰、语义明确，避免模糊缩写
- 不要随意删除现有字段，除非用户明确要求

## 安全与权限
- 涉及 `token`、`uid`、登录态、用户信息时，优先考虑安全性
- 不要在前端暴露敏感信息
- 不要默认信任外部传入参数，必须做基础校验
- 涉及权限控制时，优先在云端进行校验

## 测试要求
- 每个功能完成后进行必要的手动验证
- 确保数据正确存储和读取
- 测试边界情况
- 涉及用户状态、权限、登录、会员信息等逻辑时，重点验证异常场景

## 修改原则
- 只修改与当前需求直接相关的文件
- 不要顺手重构无关代码
- 不要删除用户未明确要求删除的字段、逻辑或配置
- 如果需求涉及多个文件，先确认修改路径再动手

