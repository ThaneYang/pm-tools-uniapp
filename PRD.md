### **一、 项目背景与技术栈：为什么选择“小程序+H5”？**

在开发“项目管理师考试工具集”时，灵活性和迭代速度是核心。我们选择了一套“轻容器+重内容”的架构方案。

- **项目名称**：pm-tools-uniapp
- **架构模式**：**1 + N 模式**。一个小程序宿主（pm-tools-uniapp）作为流量入口和权限中心，内嵌多个独立的 H5 应用（如：项目管理师知识卡片、计算题模拟器、挣值管理工具、历年真题库）。
- **技术栈总览**：
  - **前端框架**：++[uniapp](https://uniapp.dcloud.net.cn/)++（Vue3 + Vite）。
  - **后端支撑**：++[uniCloud](https://uniapp.dcloud.net.cn/uniCloud/)++（支付宝云服务空间，目录是uniCloud-alipay），共用 `alipay-pm-tools` 服务空间。
  - **基础模板**：`uni-starter`。它内置了完善的用户管理、登录（微信/一键登录）、权限校验等逻辑，极大减少了基建耗时。
  - **AI 协同开发**：采用 **VS Code + Claude Code** 作为主战力，配合 **Doubao 2.0** 模型进行代码润色和逻辑重构，实现“自然语言驱动”的开发体验。

### **二、 核心架构设计**

#### **1. 数据库设计（uniCloud）**

所有应用共用 `alipay-pm-tools` 数据库，通过 `opendb-app-list` 维护 H5 应用的跳转链接和权限标识。

- **用户表**：扩展 `uni-id-users`，增加 `vip_expire_date`（会员到期时间）和 `is_vip` 字段。
- **兑换码表**：存储 code、有效时长（天）、状态（已使用/未使用）及绑定用户。

#### **2. “小程序内嵌 H5” 逻辑**

通过 `web-view` 组件实现。小程序通过 URL 参数将用户的 `token` 或 `uid` 传递给 H5，实现登录态同步。

### **三、 核心功能模块实现**

#### **【首页】应用中心**

- **动态 Banner**：通过管理后台配置，支持配置最新课程广告或活动入口。
- **应用金刚区**：采用 `uni-grid` 布局，从 `uni-app-list` 集合动态渲染图标。
  - **交互逻辑**：点击应用图标 -> 校验应用是否免费 -> 校验用户是否为会员 -> 跳转至对应 H5 `web-view` 页面。

#### **【个人中心】会员与兑换码体系**

这是项目的**核心商业闭环**，通过“兑换码”机制实现用户增长与权益核销。

- **用户画像**：展示用户头像、用户ID及会员状态（倒计时显示）。
  - 点击头像可以替换头像
- **购买会员**：打开购买会员页面
- **兑换码核销系统**：
  - **输入逻辑**：用户输入 8位唯一兑换码。
  - **云函数处理**：
    1. 校验兑换码合法性及有效状态。
    2. 根据兑换码预设的“有效期”（如 1天、7天、30 天、365 天、永久）计算截止日期。
    3. 自动更新 `uni-id-users` 表，延长用户会员时长，并标记兑换码为“已失效”。
  - **类似核销体验**：即填即用，无需复杂的支付流程，适合社群分发。

